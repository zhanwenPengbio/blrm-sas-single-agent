# Captured artifacts — t002_posterior_toxicity

These URLs (if any) are tied to one specific run and expire when that run
is reaped on the server. Re-running the bundle regenerates them. This bundle
produces no downloadable files; it writes work data sets and a PROC MEANS
listing (see output.txt).

## Datasets

| name | rows | columns |
|------|------|---------|
| out_mcmc | 60 | log_alpha, log_beta |
| post_tox | 60 | log_alpha, log_beta, pi_5, pi_10, pi_20, pi_40 |
